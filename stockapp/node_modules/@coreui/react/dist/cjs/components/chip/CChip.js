'use strict';

var tslib_es6 = require('../../node_modules/tslib/tslib.es6.js');
var React = require('react');
var PropTypes = require('prop-types');
var index = require('../../_virtual/index.js');
var useForkedRef = require('../../hooks/useForkedRef.js');
require('@popperjs/core');
var props = require('../../props.js');

const CChip = React.forwardRef((_a, ref) => {
    var { active, ariaRemoveLabel = 'Remove', children, as: Component = 'span', className, clickable, color, disabled, filter, onClick, onDeselect, onKeyDown, onRemove, onSelect, onSelectedChange, removable, removeIcon, selectable, selected, selectedIcon, size, tabIndex, value, variant } = _a, rest = tslib_es6.__rest(_a, ["active", "ariaRemoveLabel", "children", "as", "className", "clickable", "color", "disabled", "filter", "onClick", "onDeselect", "onKeyDown", "onRemove", "onSelect", "onSelectedChange", "removable", "removeIcon", "selectable", "selected", "selectedIcon", "size", "tabIndex", "value", "variant"]);
    // A filter chip is selectable by definition.
    const isSelectable = Boolean(selectable || filter);
    const chipRef = React.useRef(null);
    const forkedRef = useForkedRef.useForkedRef(ref, chipRef);
    const isSelectedControlled = selected !== undefined;
    const [_selected, setSelected] = React.useState(Boolean(selected));
    const selectedState = isSelectedControlled ? Boolean(selected) : _selected;
    React.useEffect(() => {
        if (isSelectedControlled) {
            setSelected(Boolean(selected));
        }
    }, [isSelectedControlled, selected]);
    const isFocusable = React.useMemo(() => Boolean(!disabled && (isSelectable || removable)), [disabled, isSelectable, removable]);
    const setSelectableState = (nextSelected, event) => {
        if (!isSelectable || disabled || nextSelected === selectedState) {
            return;
        }
        if (!isSelectedControlled) {
            setSelected(nextSelected);
        }
        if (nextSelected) {
            onSelect === null || onSelect === void 0 ? void 0 : onSelect(event);
        }
        else {
            onDeselect === null || onDeselect === void 0 ? void 0 : onDeselect(event);
        }
        onSelectedChange === null || onSelectedChange === void 0 ? void 0 : onSelectedChange(nextSelected, event);
    };
    const toggleSelectedState = (event) => {
        setSelectableState(!selectedState, event);
    };
    const handleRemove = (event) => {
        onRemove === null || onRemove === void 0 ? void 0 : onRemove(event);
    };
    const handleRemoveClick = (event) => {
        event.stopPropagation();
        handleRemove(event);
    };
    const handleClick = (event) => {
        if (disabled) {
            return;
        }
        if (event.target.closest('.chip-remove')) {
            return;
        }
        if (isSelectable) {
            toggleSelectedState(event);
        }
        onClick === null || onClick === void 0 ? void 0 : onClick(event);
    };
    const handleKeyDown = (event) => {
        if (disabled) {
            onKeyDown === null || onKeyDown === void 0 ? void 0 : onKeyDown(event);
            return;
        }
        switch (event.key) {
            case 'Enter':
            case ' ':
            case 'Spacebar': {
                if (isSelectable) {
                    event.preventDefault();
                    toggleSelectedState(event);
                }
                break;
            }
            case 'Backspace':
            case 'Delete': {
                if (removable) {
                    event.preventDefault();
                    handleRemove(event);
                }
                break;
            }
            // No default
        }
        onKeyDown === null || onKeyDown === void 0 ? void 0 : onKeyDown(event);
    };
    return (React.createElement(Component, Object.assign({ className: index.default('chip', {
            active: isSelectable ? selectedState : active,
            disabled,
            [`chip-${color}`]: color,
            [`chip-${size}`]: size,
            'chip-clickable': clickable || isSelectable || Boolean(onClick),
            'chip-outline': variant === 'outline',
        }, className), "data-coreui-chip-focusable": isFocusable || undefined, "data-coreui-chip-value": value }, (disabled && { 'aria-disabled': true }), (isSelectable && { 'aria-selected': selectedState }), (isFocusable && tabIndex === undefined && { tabIndex: 0 }), { onClick: handleClick, onKeyDown: handleKeyDown }, (Component === 'button' && { disabled }), rest, { ref: forkedRef }),
        filter && selectedState && (React.createElement("span", { className: "chip-check", "aria-hidden": "true" }, selectedIcon !== null && selectedIcon !== void 0 ? selectedIcon : (React.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 512 512", fill: "currentColor" },
            React.createElement("path", { d: "M425.373 89.373 196 318.745 86.627 209.373l-45.254 45.254L196 409.255l274.627-274.628z" }))))),
        children,
        removable && !disabled && (React.createElement("button", { type: "button", className: "chip-remove", "aria-label": ariaRemoveLabel, onClick: handleRemoveClick, tabIndex: -1 }, removeIcon !== null && removeIcon !== void 0 ? removeIcon : (React.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 16 16", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round" },
            React.createElement("line", { x1: "4", y1: "4", x2: "12", y2: "12" }),
            React.createElement("line", { x1: "12", y1: "4", x2: "4", y2: "12" })))))));
});
CChip.propTypes = {
    active: PropTypes.bool,
    ariaRemoveLabel: PropTypes.string,
    as: PropTypes.elementType,
    children: PropTypes.node,
    className: PropTypes.string,
    clickable: PropTypes.bool,
    color: props.colorPropType,
    disabled: PropTypes.bool,
    filter: PropTypes.bool,
    onDeselect: PropTypes.func,
    onRemove: PropTypes.func,
    onSelect: PropTypes.func,
    onSelectedChange: PropTypes.func,
    removable: PropTypes.bool,
    removeIcon: PropTypes.node,
    selectable: PropTypes.bool,
    selected: PropTypes.bool,
    selectedIcon: PropTypes.node,
    size: PropTypes.oneOf(['sm', 'lg']),
    value: PropTypes.string,
    variant: PropTypes.oneOf(['outline']),
};
CChip.displayName = 'CChip';

exports.CChip = CChip;
//# sourceMappingURL=CChip.js.map
